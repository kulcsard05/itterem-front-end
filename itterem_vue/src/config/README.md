# Config

Purpose: central application constants and static configuration.

Scope:
- Roles and auth event names
- Order status constants and badge class mapping
- Storage keys
- Locale defaults and supported locales
- Shared timeout and UI sizing constants

Rules:
- Keep this folder side-effect free.
- Do not add runtime logic here.
- Prefer adding new constants here instead of scattering literals.
