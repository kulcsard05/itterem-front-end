# Common Components

Purpose: highly reusable UI primitives shared across multiple feature areas.

Scope:
- Small presentational components (status badges, alert blocks, etc.).

Rules:
- Keep these components stateless or minimally stateful.
- Prefer configurable props over feature-specific assumptions.
